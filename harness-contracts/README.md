# harness-contracts

本包暂时保留 Stage 2/3 所需的 Context、Memory、Observation 和错误契约。Stage-1 的产品执行
DTO 已迁移到 `financeclaw/contracts`。

Capability、Provider、Selection、Retry、Approval、ResultEnvelope 与旧 RequestTarget 已删除；
框架内部对象不再复制为通用 Harness contract。
