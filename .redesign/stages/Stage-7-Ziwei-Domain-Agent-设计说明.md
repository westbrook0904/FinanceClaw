# 紫微领域 Agent 候选

紫微能力是顶层 ReAct 调用的内部 Worker Tool，当前发布 ziwei_doushu_agent@2.1.0。输入为明确任务和结构化出生／时间参数，输出为可信盘面与自由解读文本。Worker 不读根 Journal 或长期 Memory，复用根权限和预算。

五个只读盘面工具、确定性输入校验、规则绑定、引擎、证据与文本预算保留；候选默认关闭，限 development/test。详细口径、权限、合成验证和启用配置见 [候选手册](../../docs/operations/ziwei-agent.md)。
